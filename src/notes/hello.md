---
title: hello
date: 2026-06-05
public: false
---

placeholder note — delete me.

```js
const published = notes.filter((n) => n.share);
console.log(`shipping ${published.length} notes`);
```

inline `code` too.
